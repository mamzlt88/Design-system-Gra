# Buttons Family - Step 06 Plan

**Status**: Draft, ready for designer review
**Step**: 06 Component Development Planning
**Source**: Figma `UI_Kit_(In progress)` -> Buttons family

In plain words: this step is the checklist before we build. It turns the Figma components into clear decisions about what each component should do, what options it should have in Storybook, and what needs a designer to confirm.

## Batch 01

This first batch keeps the work small enough to review:

| Planned component | Figma source | Planning file |
|-------------------|--------------|---------------|
| `Button` | `Buttons` (`50:6188`) | `Button.planning.md` |
| `IconButton` | `03 Icon Button` (`91:4104`) | `IconButton.planning.md` |
| `AmountSelector` | `Amount Selector` (`8273:7829`) | `AmountSelector.planning.md` |
| `DocumentUploadSlot` | `Document Upload Slot` (`9304:2207`) | `DocumentUploadSlot.planning.md` |
| `ExternalLinkButton` | `External Link Button` (`9182:7501`) | `ExternalLinkButton.planning.md` |

## Not In This Batch Yet

These stay in the Buttons family backlog for the next pass:

- `Pill Buttons`
- `Value Adjuster Button`
- `Information Button`

## What Triggers The Next Step

When this batch is approved, the agent can move to Step 07 and create the React components, Storybook stories, and docs. If something is unclear, the agent should ask one focused question before coding.
