# Input Components Family - Step 06 Plan

**Status**: Implemented in Storybook, ready for designer review and refinement
**Step**: 06 Component Development Planning
**Source**: Figma `UI_Kit_(In progress)` -> Input-related components

In plain words: this group records controls where a person enters, adjusts, or selects a value. `AmountSelector` is now available in Storybook.

## Batch 01

| Planned component | Figma source | Planning file |
|-------------------|--------------|---------------|
| `AmountSelector` | `Amount Selector` (`8273:7829`) | `AmountSelector.planning.md` |

## What Triggers The Next Step

When this batch is approved, the agent can use `AmountSelector` inside larger forms, calculators, or amount-selection flows. The Storybook docs page should keep amount states grouped in one canvas and use HTML tables for reference sections.
