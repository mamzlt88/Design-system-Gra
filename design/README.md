# Design Inputs

Add a Figma JSON snapshot here during Setup Integrations, or switch the agent config to Figma API mode.

The raw `figma-file.snapshot.json` file is intentionally kept out of Git because it is large and can be regenerated from the configured Figma API integration. Commit the generated tokens, component backlog, icon inventory, and Storybook implementation as the reviewable source of truth.
