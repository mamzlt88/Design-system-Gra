import { Meta, Canvas, Controls, Stories } from '@storybook/addon-docs/blocks';
import * as TooltipStories from './Tooltip.stories';

<Meta of={TooltipStories} />

# Tooltip

> **Atomic Level**: Atom | **Status**: Draft | **Figma**: [View in Figma](https://www.figma.com/design/LuOSourQp644YKhg0MrCE0/UI_Kit_-In-progress-?node-id=7443-9977)

## Overview

Displays helper text with sourced top or bottom arrow placement and alignment variants.

**Use this component when:**
- The surrounding workflow matches the sourced Figma component purpose.
- The visible state maps to a documented Figma variant.
- The component can stay presentational and controlled by props.

**Do not use this component when:**
- The behavior would require fetching, routing, storage, auth, or submission logic inside the component.
- The required state is not represented by Figma evidence or explicit config.
- A more specific existing component covers the use case.

## Default Usage

<Canvas of={TooltipStories.TopLeft} />

```tsx
import { Tooltip } from '@design-system/components';

<Tooltip arrowPlacement="bottom" arrowAlignment="middle" />
```

## Props

<Controls />

## Examples

<Canvas of={TooltipStories.PlacementExamples} />

## Accessibility

<table>
  <thead>
    <tr>
      <th>Expectation</th>
      <th>Implementation</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Accessible name</td>
      <td>Provide visible text or an explicit label prop when the visual alone is not enough</td>
    </tr>
    <tr>
      <td>Semantic role</td>
      <td>Uses native controls or explicit roles based on the represented pattern</td>
    </tr>
    <tr>
      <td>State</td>
      <td>Visual state is controlled by props and does not own application state</td>
    </tr>
    <tr>
      <td>Interaction</td>
      <td>Interactive behavior is limited to explicit event props supplied by the parent</td>
    </tr>
  </tbody>
</table>

## Related Components

<table>
  <thead>
    <tr>
      <th>Component</th>
      <th>Use it when</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>Button</code></td>
      <td>The user needs to trigger an action</td>
    </tr>
    <tr>
      <td><code>StatusBadge</code></td>
      <td>The UI needs a visible textual status</td>
    </tr>
    <tr>
      <td><code>IconButton</code></td>
      <td>The action is compact and icon-led</td>
    </tr>
  </tbody>
</table>

## Figma Mapping

<table>
  <thead>
    <tr>
      <th>Figma Property</th>
      <th>Prop</th>
      <th>Notes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>Arrow_Placement=Top</code></td>
      <td><code>arrowPlacement="top"</code></td>
      <td>Arrow above container</td>
    </tr>
    <tr>
      <td><code>Arrow_Placement=Bottom</code></td>
      <td><code>arrowPlacement="bottom"</code></td>
      <td>Arrow below container</td>
    </tr>
    <tr>
      <td><code>Arrow_Alignment</code></td>
      <td><code>arrowAlignment</code></td>
      <td>Left, middle, or right</td>
    </tr>
    <tr>
      <td><code>Text</code></td>
      <td><code>text</code></td>
      <td>Tooltip message</td>
    </tr>
  </tbody>
</table>

## All Stories

<Stories />
