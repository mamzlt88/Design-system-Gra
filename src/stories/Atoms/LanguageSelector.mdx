import { Meta, Canvas, Controls, Stories } from '@storybook/addon-docs/blocks';
import * as LanguageSelectorStories from './LanguageSelector.stories';

<Meta of={LanguageSelectorStories} />

# LanguageSelector

> **Atomic Level**: Atom | **Status**: Draft | **Figma**: [View in Figma](https://www.figma.com/design/LuOSourQp644YKhg0MrCE0/UI_Kit_-In-progress-?node-id=9274-12691)

## Overview

Displays a selectable language option with sourced default, pressed, and selected states.

**Use this component when:**
- The surrounding workflow matches the sourced Figma component purpose.
- The visible state maps to a documented Figma variant.
- The component can stay presentational and controlled by props.

**Do not use this component when:**
- The behavior would require fetching, routing, storage, auth, or submission logic inside the component.
- The required state is not represented by Figma evidence or explicit config.
- A more specific existing component covers the use case.

## Default Usage

<Canvas of={LanguageSelectorStories.Default} />

```tsx
import { LanguageSelector } from '@design-system/components';

<LanguageSelector state="selected" langText="Eng" />
```

## Props

<Controls />

## Examples

<Canvas of={LanguageSelectorStories.StateExamples} />

## Accessibility

<table>
  <thead>
    <tr>
      <th>Expectation</th>
      <th>Implementation</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Accessible name</td>
      <td>Provide visible text or an explicit label prop when the visual alone is not enough</td>
    </tr>
    <tr>
      <td>Semantic role</td>
      <td>Uses native controls or explicit roles based on the represented pattern</td>
    </tr>
    <tr>
      <td>State</td>
      <td>Visual state is controlled by props and does not own application state</td>
    </tr>
    <tr>
      <td>Interaction</td>
      <td>Interactive behavior is limited to explicit event props supplied by the parent</td>
    </tr>
  </tbody>
</table>

## Related Components

<table>
  <thead>
    <tr>
      <th>Component</th>
      <th>Use it when</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>Button</code></td>
      <td>The user needs to trigger an action</td>
    </tr>
    <tr>
      <td><code>StatusBadge</code></td>
      <td>The UI needs a visible textual status</td>
    </tr>
    <tr>
      <td><code>IconButton</code></td>
      <td>The action is compact and icon-led</td>
    </tr>
  </tbody>
</table>

## Figma Mapping

<table>
  <thead>
    <tr>
      <th>Figma Property</th>
      <th>Prop</th>
      <th>Notes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>State=Default</code></td>
      <td><code>state="default"</code></td>
      <td>Default language option</td>
    </tr>
    <tr>
      <td><code>State=Pressed</code></td>
      <td><code>state="pressed"</code></td>
      <td>Pressed visual state</td>
    </tr>
    <tr>
      <td><code>State=Selected</code></td>
      <td><code>state="selected"</code></td>
      <td>Selected radio state</td>
    </tr>
    <tr>
      <td><code>Lang_Text</code></td>
      <td><code>langText</code></td>
      <td>Visible language text</td>
    </tr>
  </tbody>
</table>

## All Stories

<Stories />
